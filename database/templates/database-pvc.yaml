apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.databasepvc.pvcname }}
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: {{ .Values.databasepvc.classname }}
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi

